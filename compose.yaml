services:
  app:
    container_name: portafolio
    build:
      context: ./app
    ports:
      - 5173:5173
    volumes:
      - ./app:/app
      - /app/node_modules
    environment:
      - NODE_ENV=development
